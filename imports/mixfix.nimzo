-- todo list of possible mixfixes (ie. because reserved && check ambiguous parse)
--i_t_e_ = \ x a b => if x then a else b
--isBool a x = i a t a e x

_%_ = plus

f = 3 % 2 % 1
--
_X_ = \a b => plus a b
--g = 3 X 2
--a = 2 X_
--b = _X 2 -- TODO
