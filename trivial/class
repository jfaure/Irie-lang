type Float     = #!Float

class Num where {
  (+) : Num->Num->Num
  (-) : Num->Num->Num
  }

-- Int subsumes Num
instance Num Int where {
  (+) : Int->Int->Int= #!add
  (-) : Int->Int->Int= #!sub
  }
