alloc   : Int -> a   = #!alloc
--sizeof : Set -> Int = #!sizeof
--szInt = sizeof Int

-- CArray ty = (#!ptr ty)
--mkArray : Int -> (CArray ty)
--mkArray count = CArray (alloc (count + 4))

(!) : (#!ptr ty) -> Int -> ty = #!extractVal
(=!) : (#!ptr ty) -> Int -> ty -> (#!ptr ty) = #!insertVal

f = "hello"
main = printf "%c\n" ("ab" ! (1+0))
