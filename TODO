# expr2Core
  * GADT's (apparently tricky because of equality constraints eg. Sum a a => a == a)
  * parsing duplicate fn defs
  * arity typecheck

# Low priority
  * error reporting
  * parser source locations
  * parser linefolds
  * precompiled modules (sv them where?)

# Maybe
  * rec (let-rec)
