# High
  * top level function aliases need explicit args in llvm; eg. `match = \case ..`
# Low priority
  * error reporting
  * parser source locations
  * parser linefolds
  * precompiled modules
  * rec
